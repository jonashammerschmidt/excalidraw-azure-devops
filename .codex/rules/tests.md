# Tests

- Keep tests next to the source they cover and follow the established Angular
  test setup and naming style in the repository.
- Structure test bodies with Arrange, Act, and Assert when that makes the
  scenario clearer.
- Cover observable behavior, especially project scoping, version conflicts,
  and autosave behavior when changing those areas.
- Run `npm run test` for changed logic, state handling, persistence, or tests.
